name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Configure with cmake
      run: ls
    - name: Configure with cmake
      run: cmake .
    - name: building
      run: make 
